namespace Bit2sky.Domain.Enums;

// admin.permissions.action CHECK constraint (Section 6 / Section 3A).
public enum PermissionAction
{
    View,
    Create,
    Update,
    Delete,
    Export,
    Approve,
    Assign,
    Manage,
    Broadcast
}
